{
  "legacyWatch": true,
  "watch": ["../common", "./"],
  "ext": "js,ts,json",
  "exec": "yarn turbo run build:dev && node --inspect=0.0.0.0 -r ts-node/register src/bin/www.ts",
  "events": {
    "start": "yarn turbo run lint"
  }
}